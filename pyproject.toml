[project]
name = "heygen-openai-pipecat-demo"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "fastapi~=0.115.0 ; extra == 'websocket'",
    "flask==3.0.3",
    "pipecat-ai[deepgram,elevenlabs,openai,silero,websocket]==0.0.67",
    "python-json-logger==2.0.2",
    "requests==2.32.3",
    "uvicorn==0.30.1",
]

[dependency-groups]
dev = [
    "poethepoet>=0.34.0",
]

[tool.poe.tasks]
start = "python main.py"